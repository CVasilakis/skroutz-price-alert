# Dependencies specific to the Skroutz scraper. Installed only when this plugin
# is provisioned (./install.sh or ./install.sh --skroutz), so an install that
# skips Skroutz never pulls its transport stack. tls-client backs the shared
# HttpScraperClient this plugin's client extends.
tls-client
