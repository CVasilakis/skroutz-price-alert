# border: red
# exit: 15

[+] Update checks
    [v] The checkout and installed target selection are safe to update.

[+] Source update
    [v] Fetched origin/main before stopping any target.
    [v] Verified origin/main can safely fast-forward this checkout.

[+] Target quiescence
    [v] Captured the installed unit files and timer states.
    [v] [alpha] Safely stopped and disabled its timer.
    [v] [beta] Safely stopped and disabled its timer.

[+] Source advancement
    [i] Advancing the checkout with a verified fast-forward.
    [v] Fast-forwarded the project files to origin/main.

[+] JSON migration
    [v] Managed JSON documents are ready for the updated source.

[+] Installation checks
    [v] The project venv path is safe.
    [v] System Python 3.10 or newer is available.
    [v] The existing Python environment uses a supported Python.
    [v] Python venv support is available.
    [v] Systemd user services are available.

[+] Python environment
    [i] Updating the existing Python environment.
    [v] The Python environment is ready.
    [v] Packaging tools updated.
    [v] Updated core dependencies.
    [v] Core dependencies are compatible.

[+] Target provisioning
    [x] [beta] Remove unsupported keys from `config/beta.json`.
    [i] [beta] Existing systemd units were left unchanged.
    [v] Configured timers for the selected targets.

[+] Installation result
    [x] One or more targets were skipped because their configuration is invalid.
    [i] Fix each reported target configuration, then run ./install.sh again.

[+] Timer activation
    [i] Restoring eligible target timer states.
    [v] [alpha] Restored its prior timer state.
    [v] [beta] Restored its prior timer state.

[!] Update result
    [!] Update complete, but one or more targets retained their existing units because
        their configuration is invalid.
    [!] Fix each reported target configuration, then rerun ./update.sh.
