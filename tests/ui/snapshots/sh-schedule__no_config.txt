# border: green
# exit: 0

[skroutz] No config file found; timer left unchanged.
Copy <BASE_DIR>/src/core/scrapers/plugins/skroutz/config.example.json to config/skroutz.json to
configure it.

All timers already match their configured intervals.
