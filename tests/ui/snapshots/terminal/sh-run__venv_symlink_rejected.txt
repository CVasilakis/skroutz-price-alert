# border: red
# exit: 1

[+] Run preflight
    [x] The project venv path must be a project-owned directory, not a symlink.
    [!] Remove the venv symlink, then recreate it with ./scripts/dev/setup.sh or ./scrooge-alert
        install.
