# border: red
# exit: 1

[+] Installation checks
    [v] The project venv path is safe.
    [v] System Python 3.10 or newer is available.
    [v] The user command destinations are safe.
    [x] The existing Python environment is unusable.
    [i] Run ./scripts/uninstall.sh, then run ./install.sh again.
