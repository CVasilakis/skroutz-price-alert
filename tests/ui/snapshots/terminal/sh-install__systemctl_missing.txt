# border: red
# exit: 1

[+] Installation checks
    [v] The project venv path is safe.
    [v] System Python 3.10 or newer is available.
    [v] The user command destinations are safe.
    [v] The existing Python environment uses a supported Python.
    [v] Python venv support is available.
    [x] Systemd user services are not available.
    [i] Install or enable systemd user services, then run ./install.sh again.
